# Copy to .env (git-ignored). Never commit real values.
# Use the Supabase project URL and an ANON key restricted by RLS policies
# to insert/update-only on the tickets table (see supabase/schema.sql).
# Never put the service_role key in this desktop app.
SUPABASE_URL=
SUPABASE_KEY=
